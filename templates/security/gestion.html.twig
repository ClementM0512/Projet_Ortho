{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block body %}
<div class="form-srch">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Valider</button>
    {{ form_end(form) }}
</div>
    <div class="btn-admin">
	<a href="{{ path('security_register') }}">
			<button type="submit" class="btn btn-primary">
				Création d'un nouvel utilisateur
			</button>
			</a>
		</div>
		
<center>Liste des utilisateurs</center>

{% for Resultat in rechercheResultat %}
<div class="listes">
		<a href="{{ path('security_userEdit', {'id': Resultat.id}) }}">
			<button type="submit" class="btn btn-outline-primary btn-patient" >
	{{ Resultat.username }}/ {{ Resultat.email }}/ {{ Resultat.nom }}/{{ Resultat.prenom }}
	</button>
			</a>
		</div>
{% endfor %}
    
	
    {% for user in Users %}
		<div class="listes">
		<a href="{{ path('security_userEdit', {'id': user.id}) }}">
			<button type="submit" class="btn btn-outline-primary btn-patient" >
			{{ user.username}}	
			{{ user.email}}			
			</button>
			</a>
		</div>
    {% endfor %}
{% endblock %}