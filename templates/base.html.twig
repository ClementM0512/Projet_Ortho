<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>{% block title %}Bouge tes yeux{% endblock %}</title>
{% block stylesheets %}
<link rel="stylesheet" href="\CSSFiles\stylesbootswatch.css">
<link rel="stylesheet" href="\CSSFiles\app.css">
{% endblock %}
</head>
<body>
{% if app.user %}
	<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
		<a class="navbar-brand" href="{{ path('home') }}">Bouge tes yeux</a>

		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav mr-auto" id="change-active">
{# 				{% if is_granted('ROLE_ADMIN') or is_granted('ROLE_SUPERADMIN') %}#}
				
				<li class="nav-item active change-item"><a class="nav-link"
					href="{{ path('home') }}">Home</a>
					</li>
					<li class="nav-item "><a class="nav-link"
					href="{{ path('patients') }}">Patients</a>
					</li>
					<li class="nav-item "><a class="nav-link"
					href="{{ path('listeExos') }}">Exercices</a>
					</li>
					<li class="nav-item "><a class="nav-link"
					href="{{ path('security_newPassword') }}">changer le mot de passe</a>
					</li>
					{% if is_granted('ROLE_SUPERADMIN') %}
					<li class="nav-item"><a class="nav-link"
					href="{{ path('security_admin') }}">Admin</a>
					</li>
				    {% endif %}
{# 				{% endif %}#}
				<li class="nav-item"><a class="nav-link"
					href="{{ path('security_logout') }}">Déconnexion</a></li>
			</ul>
		</div>
{% block titre %}{% endblock %}
	</nav>
	
{% endif %}
	<div class="content">
		{% block body %}{% endblock %}
	</div>

	{% block javascripts %}<script src="/JS/app.js" />{% endblock %}
</body>
</html>
