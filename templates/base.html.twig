<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>{% block title %}Bouge tes yeux{% endblock %}</title>
<link rel="stylesheet" href="\CSSFiles\stylesbootswatch.css">
<link rel="stylesheet" href="\CSSFiles\app.css">
{% block stylesheets %}
{% endblock %}
</head>
<body>
{% if app.user %}
	<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
		<a class="navbar-brand" href="{{ path('home') }}">Bouge tes yeux</a>

		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav mr-auto">
{# 				{% if is_granted('ROLE_ADMIN') or is_granted('ROLE_SUPERADMIN') %}#}
				
				<li class="nav-item active"><a class="nav-link"
					href="{{ path('home') }}">Home</a>
					</li>
					<li class="nav-item active"><a class="nav-link"
					href="{{ path('patients') }}">Patients</a>
					</li>
{# 					{% if is_granted('ROLE_SUPERADMIN') %}#}
{# 					<li class="nav-item"><a class="nav-link"#}
{# 					href="{{ path('db_Users') }}">Users</a>#}
{# 					</li>#}
{# 				    {% endif %}#}
{# 				{% endif %}#}
				<li class="nav-item"><a class="nav-link"
					href="{{ path('security_logout') }}">Déconnexion</a></li>
			</ul>
		</div>

	</nav>
{% endif %}
	<div class="content">
		{% block body %}{% endblock %}
	</div>

	{% block javascripts %}{% endblock %}
</body>
</html>
