<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no, target-densitydpi=device-dpi">
<title>{% block title %}Bouge tes yeux{% endblock %}</title>
{% block stylesheets %}
<link rel="stylesheet" href="\CSSFiles\stylesbootswatch.css">
<link rel="stylesheet" href="\CSSFiles\app.css">
<link rel="stylesheet" href="\CSSFiles\allfontawesome.css">
{% endblock %}
</head>
<body>
{% if app.user %}
	<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
		<a class="navbar-brand" href="{{ path('patients') }}">Bouge tes yeux</a>

		<div class="collapse navbar-collapse" id="navbarColor01 dropMenuTotal">
			<i class="fas fa-bars" id="iconMenu" onclick="OpenMenu()"></i>
			<ul class="navbar-nav mr-auto" id="change-active dropMenu">

					<li class="nav-item "><a class="nav-link"
					href="{{ path('patients') }}">Patients</a>
					</li>
					<li class="nav-item "><a class="nav-link"
					href="{{ path('listeExos') }}">Exercices</a>
					</li>
					<li class="nav-item "><a class="nav-link"
					href="{{ path('security_newPassword') }}">changer le mot de passe</a>
					</li>
					{% if is_granted('ROLE_SUPERADMIN') %}
					<li class="nav-item"><a class="nav-link"
					href="{{ path('security_admin') }}">Admin</a>
					</li>
				    {% endif %}
{# 				{% endif %}#}
				<li class="nav-item"><a class="nav-link"
					href="{{ path('security_logout') }}">Déconnexion</a></li>
				
			</ul>
			
		</div>
{% block titre %}{% endblock %}
	</nav>
	
{% endif %}
	<div class="container">
		{% for message in app.flashes('danger') %}
           		<div class = "alert alert-danger flashmessage"> {{ message }}</div> 
            {% endfor %}
		{% block body %}{% endblock %}
	</div>

{# 	{% block javascripts %}#}
{# 	<script src="/JS/app.js"/>#}
{# 	<script src="/JS/allfontawesome.js"/>#}
{# 	{% endblock %}#}
</body>
</html>