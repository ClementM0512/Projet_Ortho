{% extends 'base.html.twig' %}


{% block body %}
<section class="patients">
	<a href="{{ path('patientCreate')}}" class="btn btn-success btn-lg btn-create">Ajouter un patient</a>
	
	{% for patient in patient %}
		<div class="metadata">
		<h2>
			{{ patient.nom}}
			{{ patient.prenom}}
			{{ patient.birthdate | date('d/m/y')}}		{# Différent champ de la classe patient avec un formatage a l'aide d'un filtre #}
		</h2>
		</div>
	{% endfor %}

</section>
{% endblock %}
